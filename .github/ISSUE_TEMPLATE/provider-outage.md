---
name: Provider outage
about: An AI/generation provider (KIE, Alibaba, OpenRouter) is down, degraded, or erroring
---

## Provider
Which provider is affected (e.g. KIE, Alibaba Cloud, OpenRouter):

## Model IDs
Specific model(s)/endpoint(s) affected (e.g. slugs used in `src/lib/models.js` /
`src/lib/providers.js`):

## Error codes / messages
Exact HTTP status codes and error bodies/messages seen (redact API keys):

## Start time
When the errors started (with timezone) and whether they're ongoing or intermittent:

## User impact
- What breaks for users (e.g. generation requests fail, credits reserved but not
  settled, specific tool/model unusable):
- Roughly how many users/requests affected, if known:
- Any workaround in place (e.g. fallback provider, feature disabled):

## Evidence
Logs, PM2 output, provider status page links, sample request/response.
